<div class="d-flex justify-content-center">
    <div class="btn-group mb-4" role="group" aria-label="Basic example">
        <a class="btn btn-outline-secondary" href="{{ path('list_pitch_club', {clubId: pitch.club.id}) }}"><i class="fas fa-arrow-left"></i> Club</a>
        <a class="btn btn-outline-secondary {% if app.request.attributes.get('_route') == 'page_pitch' %}active{% endif %}" href="{{ path('page_pitch', {pitchId: pitch.id}) }}">Informations</a>
    </div>
</div>